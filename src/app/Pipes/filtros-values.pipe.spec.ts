import { FiltrosValuesPipe } from './filtros-values.pipe';

describe('FiltrosValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrosValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
